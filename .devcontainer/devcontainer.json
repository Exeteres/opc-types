{
  "dockerComposeFile": "docker-compose.yaml",
  "service": "main",
  "workspaceFolder": "/home/dev/workspace",
  "remoteUser": "dev",
  "onCreateCommand": "corepack enable && (yes | yarn config set --home enableTelemetry 0) && yarn"
}